# RC beam tests

Run from the repository root with Node.js:

```sh
node --test tests/rc-beam.test.cjs tests/rc-beam-ui.test.cjs
```

The UI smoke tests use a minimal DOM adapter and do not verify visual layout.
