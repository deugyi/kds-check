# 내수압 슬래브 — 지하 바닥판 양압력

독립기초로만 지지되는 지하 바닥슬래브가 지하수 양압력과 중력하중을 받을 때의 **휨 철근**을 검토한다. 기둥 그리드 교점마다 독립기초가 있고 지중보나 벽체는 없는 배치를 대상으로 한다.

## 하중

요청한 두 조합을 개별 또는 동시 검토한다. 하중의 작용 방향을 구분하며 전체 KDS 하중조합을 생성하는 기능은 아니다.

```
D = 슬래브 자중 + 무근 콘크리트 높이(mm) / 1000 × 23
H = γw · hw
상향: qu = 1.6H − 0.9D
하향: qg = 1.2D + 1.6L
```

- 물 9.81 kN/m³, 철근콘크리트 24 kN/m³, 무근 콘크리트 23 kN/m³는 고정값이며 하중 산정에 표시한다. 지하수위 hw는 슬래브 하면 기준이다.
- 무근 콘크리트 높이는 기본 150 mm이며 3.45 kN/m²로 환산해 계산 엔진의 qsd에 전달한다. 0 mm도 허용한다.
- L은 별도 입력하며 기본값은 3 kN/m²이다.
- 상향 조합에 L을 저항으로 넣지 않는다. qu≤0이면 상향 검토를 생략하되 중력 조합이 선택돼 있으면 계속 계산한다.
- 각 조합의 Mo를 따로 구하고 X·Y, 주열대·중간대, 상·하부별 최대 Mu를 사용한다. 결과에 지배 조합과 위치를 표시한다.

## 해석

KDS 14 20 70 4.1.3 직접설계법을 사용한다.

- 순경간 ln은 **기초면 사이 거리**이다. 지지점이 독립기초이므로 기둥 치수는 쓰지 않는다. 4.1.3.2(5)에 따라 ln ≥ 0.65 l 을 하한으로 둔다. 기초를 키우면 ln이 짧아져 Mo와 모든 설계대의 Mu가 줄지만, 기초면 사이 거리가 0.65 l 아래로 내려가면 하한이 지배해 **그 뒤로는 기초를 더 키워도 모멘트가 변하지 않는다**. 받침부를 크게 잡아 설계경간을 무한정 줄이지 못하도록 기준이 감소폭을 35%로 제한한 것이다. 하한이 지배하면 화면에 그 사실과 함께 모멘트가 더 이상 줄지 않는다는 점을 표시한다.
- Mo = qu · l2 · ln² / 8 — 식 (4.1-2). X·Y 두 방향을 각각 계산한다.
- 경간 분배 — 내부 경간은 부 0.65 / 정 0.35 (4.1.3.3(2)). 단부 경간은 표 4.1-1의 5개 경우 중 선택한다.

| 단부 경간 조건 | 내부 받침부 부 | 정 | 외부 받침부 부 |
|---|---|---|---|
| ① 구속되지 않은 외부 받침부 | 0.75 | 0.63 | 0 |
| ② 모든 받침부 사이에 보가 있는 슬래브 | 0.70 | 0.57 | 0.16 |
| ③ 보 없는 슬래브 · 테두리보 없음 | 0.70 | 0.52 | 0.26 |
| ④ 보 없는 슬래브 · 테두리보 있음 | 0.70 | 0.50 | 0.30 |
| ⑤ 완전 구속된 외부 받침부 | 0.65 | 0.35 | 0.65 |

- 주열대 폭 = 2 × min(0.25 l1, 0.25 l2), 중간대는 나머지 (4.1.2.1(2)(3)).
- 보가 없으므로 α1 = 0, βt = 0이고 표 4.1-2·4.1-3·4.1-4의 분배율이 상수로 고정된다. 내부 받침부 부모멘트 75%, 정모멘트 60%, 외부 받침부 부모멘트 100%가 주열대 몫이며 나머지가 중간대 몫이다. 이 때문에 l2/l1에 대한 보간이 필요 없다.
- 기초가 없는 패널 중앙부는 중간대가 담당한다. Mo는 패널 전체 폭에 대한 전체 정적 모멘트이므로 지지점 위치와 무관하게 평형으로 성립하며, 중앙부의 요구량은 Mo 안에 포함되어 있다.

## 평면도

입력한 그리드 경간과 기초 크기로 2×2 베이 평면을 그린다. 설계대가 패널이 아니라 그리드 선을 중심으로 잡힌다는 점을 보이기 위해 한 베이가 아니라 2×2 베이를 그린다.

- 회색 사각형이 독립기초, 파란 사각형이 기둥, 검은 점선이 기둥 그리드이다. 기둥 크기는 표시용 입력이며 계산에 쓰지 않는다.
- 파란 띠가 주열대, 녹색 띠가 중간대이고 선택한 방향의 설계대만 표시한다. 방향은 버튼으로 전환한다.
- 띠를 클릭하면 검토표에서 같은 방향·설계대의 행이 강조되고, 검토표의 행을 클릭하면 반대로 평면도의 띠가 강조된다. 같은 대상을 다시 누르면 해제된다.
- 치수는 그리드 경간 l1·l2, 기초 크기, 기초면 사이 거리를 표시한다. 0.65 l 하한이 지배하면 그림 아래에 순경간과 함께 표시한다.
- 각 설계대에 철근을 그린다. **상부근은 실선(T)**, **하부근은 점선(B)**이고 둘 다 전 구간 연속으로 그린다. 제안 배근을 함께 표기하며 정착·이음·연장길이는 나타내지 않는다. 중간대는 주열대 양쪽에 놓이지만 하나의 설계대이고 결과도 하나이므로 철근을 한 번만 그린다. 띠 채색은 설계대 구성을 보이기 위해 양쪽 모두 남긴다. 띠가 얇아 글자가 겹칠 때는 그림의 표기를 생략하고 아래 표에만 싣는다.

## 배근 범위

양압력에서는 중앙부가 상부 인장, 기초면이 하부 인장이다. 중력하중에서는 중앙부가 하부 인장, 기초면이 상부 인장이다. 여기서 정·부 표기는 직접설계법 분배 위치를 구분하는 기존 명칭이며 실제 인장면을 함께 표시한다.

각 면에서 선택 조합과 모든 지지부 위험단면의 최대 소요강도를 고른다. 한 설계대의 상·하부를 같은 간격으로 연속 배근하는 기존 정책을 유지하며 두 면의 합으로 수축·온도 최소철근량을 검사한다. 부모멘트 위치는 받침부 면인 기초면이며 기초 내부 자체의 설계는 범위 밖이다.

## 단면 검토

폭 1,000 mm 단면의 단철근 휨으로 계산한다.

- 등가직사각형 응력분포 변수는 KDS 14 20 20 표 4.1-2를 쓰며 RC 보 모듈의 `concrete()`를 공유한다.
- 변형률 적합과 탄소성 철근응력(Es = 200,000 MPa)으로 ΣF = 0인 중립축을 이분법으로 찾는다. 압축철근은 고려하지 않는다.
- φ는 최외단 인장변형률로 산정한다 (KDS 14 20 10 4.2.3(2)).
- 최소허용변형률 미달은 조건 미달로 표시한다.

## 최소철근과 간격

- 수축·온도철근비는 fy ≤ 400 MPa이면 0.0020, 초과하면 0.0020 × 400/fy이고 어떤 경우에도 0.0014 이상이다. 단위 폭 m당 1,800 mm²를 넘길 필요는 없다 (KDS 14 20 50 4.6.2). 원문에서 fy > 400인 경우의 식은 문서 변환 과정에서 수식 객체로 빠져 표준 형태인 0.0020 × 400/fy로 구현했으며, 명시된 하한 0.0014와 정합한다.
- 이 철근비는 **콘크리트 전체 단면적에 대한 비**이므로 상부근과 하부근의 단면적을 **더해서** 검사한다. 각 면이 개별로 최소철근량을 만족할 필요는 없다.
- 위험단면의 철근 간격은 슬래브 두께의 2배 이하, 300 mm 이하이다 (KDS 14 20 70 4.1.5.1(2)). 이 제한은 면마다 적용한다.
- 제안 배근은 **설계대마다 상·하부 간격을 통일**한다. 시공성 때문에 실무에서 한 열대의 상·하부를 다른 간격으로 두지 않기 때문이다. 두 면 중 무거운 쪽이 간격을 정하며, 그 간격에서 상·하부 모두 φMn ≥ Mu와 간격 제한을 만족하고 두 면의 합이 단면 최소철근을 넘는 가장 넓은 간격을 고른다. 후보 간격은 100·125·150·175·200·250·300 mm이며, 선택한 규격으로 어떤 간격에서도 성립하지 않으면 `배근 불가`로 표시한다.
- 각 조합에서 상·하부 소요강도를 비교한 뒤 더 엄격한 쪽이 공통 간격을 결정한다.

## 기준

| 문서 | 조항 |
|---|---|
| KDS 14 20 70 : 2021 | 4.1.2.1(2)(3) 설계대, 4.1.3 직접설계법, 식 4.1-2, 표 4.1-1~4.1-4, 4.1.5.1(2) 간격 |
| KDS 14 20 20 : 2022 | 4.1.1 및 표 4.1-2 응력블록, 4.1.2 변형률 한계 |
| KDS 14 20 10 : 2021 | 4.2.3(2) 강도감소계수, 4.3.3 Es |
| KDS 14 20 50 : 2022 | 4.6.2 수축·온도철근 |

## 검사

`node --test tests/*.test.cjs`

하중 산정, 순경간 하한, Mo와 분배, 설계대 폭, 인장면 부호, 단철근 폐합식 대조, 표 4.1-1의 5개 경우, 최소철근비와 상한, 제안 배근의 최적성, 오류 입력을 검사한다.

## 구현하지 않은 항목

- **전단** — 1방향 전단과 뚫림전단 모두 검토하지 않는다. 이 화면은 휨 철근만 다룬다.
- **기초판 자체의 휨·전단과 배근** — 기초면 부모멘트는 슬래브 하부근으로 검토하지만, 기초 안쪽에서의 저항과 기초의 배근은 다루지 않는다.
- 부력에 대한 구조물 전체의 부상 안정
- 균열폭 계산, 처짐, 정착·이음·단부 연장길이 상세
- 기초판 자체의 설계, 지반 반력 분포, 말뚝 반력
- 직접설계법 제한사항(4.1.3.1) 중 장변/단변 비와 중력하중 조합의 L≤2D를 확인하며 제한을 벗어나면 배근 제안을 제공하지 않는다. 각 방향 3경간 이상 연속, 인접 경간차 긴 경간의 1/3 이하, 기둥 어긋남 10% 이하는 사용자가 확인해야 한다.
- 기초 배치가 불규칙하거나 패널이 매우 큰 경우에는 등가골조법이나 유한요소해석이 적합하다. 직접설계법은 설계대 평균 모멘트만 제공하며 패널 내 점별 분포를 주지 않는다.
- 기초는 정사각형으로 가정한다. 직사각형 기초는 지원하지 않는다.
