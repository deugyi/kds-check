# 2026-09-10 검증 및 보완 기록

검토 기준 버전: 2ab8837. 이번 보완은 합성보 스터드 배치 입력, 지하 슬래브의 두 하중조합 비교, 문서·좁은 화면·인쇄 보완이다.

## 원문 대조

사용자가 제공한 `KDS 14 31 80 합성구조 부재 설계기준(하중저항계수설계법).docx`의 본문 및 Office Math 수식 객체를 읽었다. 4.5.2(2)의 3.76√(E/Fy), 식 4.5-1a~c, 식 4.8-1과 골데크 없는 경우의 Rg=1.0, Rp=0.75가 기존 구현과 일치했다. 4.8.2.4의 길이방향 6d, 직각방향 4d, 최대 min(8ts,900 mm)를 대조했다. 기존 화면의 ‘수식 누락’ 안내를 대조 완료로 바꾸었다. 원문 문서는 저장소에 포함하지 않는다.

`KDS 14 20 70 콘크리트 슬래브와 기초판 설계기준_수정.docx`에서 직접설계법 제한 4.1.3.1(6)의 등분포 연직하중·L≤2D, 4.1.3.2(5)의 순경간 0.65l 하한, 내부 경간 0.65/0.35 분배를 확인했다. 직접설계법을 독립기초 지지 바닥판에 적용하는 해석 가정은 기존대로 유지한다. 실제 지지·연속 조건은 개별 프로젝트에서 확인해야 한다.

## 수정한 판정과 입력

- 스터드: 한 줄 개수·종방향 간격·횡방향 간격을 입력한다. 강도에는 한쪽 반 경간의 스터드만 사용하고 실제 전 경간 개수도 표시한다. 중앙 간격, 횡간격, 플랜지 내 배치와 기존 길이·직경 제한을 확인한다. 상세조건 실패를 전체 판정에 반영했다.
- 기존 개수 입력 엔진의 간격 환산은 전 경간을 사용하던 것을 반 경간 기준으로 바로잡았다. 기존 40개/반 경간, L=9m, 단일 종열은 112.5 mm여서 19mm 스터드의 6d=114 mm보다 작다. 이전 ‘상세 충족’ 회귀값은 유지하지 않는다.
- 스터드 단면적은 이형철근 공칭치수 대신 선택한 몸체 직경(13·16·19·22mm)의 πd²/4이다. 납품 제품의 실제 치수·재료값을 확인해야 한다.
- 슬래브: `1.6H−0.9D` 상향과 `1.2D+1.6L` 하향을 별도 계산하고 각 방향·설계대·면의 최대 소요강도를 선택한다. 상향하중이 없어도 중력 조합은 검토한다. L은 0으로 초기화하되 실제 하중 입력을 안내한다. 장단변비 또는 L≤2D 제한 이탈 시 배근 제안을 숨긴다.
- 오래된 README의 J·Mu 설명과 기둥의 축별 검토 범위 안내를 정리했다. X·Y 개별 검토는 동시 2축 휨 검토를 대신하지 않는다.

## 계산 검증

`node --test tests/*.test.cjs` — 97개 검사 통과.

| 대상 | 비교 방법 및 범위 |
|---|---|
| 합성보 | 강재 단면을 0.02mm 수평 띠로 나누어 응력을 독립 수치적분. 엔진의 단면 분할·모멘트 합산 함수를 사용하지 않고 여러 합성률의 힘 평형과 φMn 대조 (허용차 4kN, 0.5kN·m) |
| RC 원형 기둥 | 0.002mm 띠로 원형 압축영역 면적·모멘트를 수치적분하고 3개 중립축 깊이의 Pn·Mn 대조 (허용차 0.02kN, 0.005kN·m) |
| RC 사각 기둥 | 기존 TI-Nspire 대조 고정값, 독립 철근응력 합산, 축별 변환 회귀검사 유지 |
| 슬래브 | D=10.65, H=19.62, L=10kN/m²에서 상향 21.807, 하향 28.78kN/m². Mo=qu×8.2×5.33²/8 및 설계대 분배를 독립 산술 대조. 인장면 반전, 단부 경간 지배단면, 두 조합 최대값 확인 |
| 입력·경계 | 스터드 줄 수의 정수 경계, 부족 간격·폭·길이, 잘못된 수치, 순 상향하중 없음, L=0, 직접설계법 제한 이탈 및 화면 연결 검사 |

이 기록은 대표 예제·회귀검증이다. 모든 가능한 단면과 실제 구조물의 설계 적합성을 인증하는 것은 아니다. 외부 상용 해석 프로그램과의 신규 대조는 수행하지 않았다.

## 화면 및 인쇄

브라우저에서 두 하중조합·스터드 입력과 미달 판정을 확인했다. 390px 폭에서는 입력란을 한 열로 표시하며 슬래브 도면 확대를 제공한다. 표는 화면에서 가로 스크롤을 사용한다.

동일 계산 스크립트로 만든 대표 입력·결과의 정적 HTML을 Chromium PDF 렌더러로 인쇄하여 RC 보·RC 기둥·철골 보·슬래브·합성보 5종을 검토했다. 인쇄에서는 표 최대높이·고정 머리글을 해제하고 셀 줄바꿈과 카드 페이지 나눔을 적용한다. 이 검사는 대표 렌더링 검증이며 사용자별 프린터 드라이버·인쇄 배율의 모든 조합을 포함하지 않는다.

## 남겨둔 검토 범위

슬래브의 1방향·뚫림전단, 부상 안정, 균열폭·처짐·정착은 별도이다. 합성보는 골데크 없는 단순보 정모멘트·중앙 최대모멘트 가정이며 실제 자유단 거리·측면 피복·머리·용접 상세·집중하중 위치별 전단연결 검토는 별도이다. 전체 설계하중 조합 생성 기능은 포함하지 않는다.
